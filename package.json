{
  "name": "rehikir-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^6.2.0",
    "sass": "^1.85.0",
    "gh-pages": "^6.3.0"
  },
  "dependencies": {
    "gsap": "^3.12.5"
  }
}

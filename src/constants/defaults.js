// Shared default values for components
export const componentDefaults = {
  page: 'home',
  inner: false,
  maxSlots: 5,
  takenSlots: 0,
  emptyLinks: [],
  emptyString: '',
  emptyChildren: ''
}

// Slot defaults
export const slotDefaults = {
  maxSlots: 5,
  takenSlots: 0
}
